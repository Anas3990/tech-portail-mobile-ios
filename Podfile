# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'tech-portail-mobile-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for tech-portail-mobile-iOS
    pod 'Firebase/Core'
    pod 'Firebase/Auth'
    pod 'FirebaseUI', '~> 4.0'
    pod 'Firebase/Firestore'
    pod 'Eureka'
 
  target 'tech-portail-mobile-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'tech-portail-mobile-iOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
